package sqltote

var (
	// Root is generated by tote.  Do not modify.
	Root = struct {
		Insert      string
		QueryAll    string
		QueryBySlug string
	}{
		`INSERT INTO post (slug, title, created, markdown, html)
VALUES (?, ?, ?, ?, ?)`,
		`SELECT *
FROM post`,
		`SELECT id, slug, title, created, markdown, html
FROM post
WHERE slug = ?`,
	}
)
